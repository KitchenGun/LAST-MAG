{
  "name": "gulag-leaderboard-smoke",
  "private": true,
  "type": "module",
  "scripts": {
    "db:local": "wrangler d1 execute gulag-leaderboard-smoke --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute gulag-leaderboard-smoke --remote --file=./schema.sql",
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
