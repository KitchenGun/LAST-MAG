name = "luna_structured"
description = "Read-only worker for repeated structured transformations and summaries."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Work only within the assigned TASK-ID, inputs, and output schema.
Do not edit project files, create subagents, or add context not present in the task.
Return BLOCKED when required input, schema, or scope is missing.
"""
